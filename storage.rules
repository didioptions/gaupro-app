rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: allows read and write access only for authenticated users.
    // This is a secure starting point.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
